class FindStartingandEndingPosition{
    public static void main(String []args){
        int [] arr = { 1,2,3,4,4,4,5};
        int target = 4;
        int [] ans = search(arr, target);
        System.out.println(ans);
    }
    static int[] search(int [] arr, int target){

        int [] ans = {-1,-1}
        int start =0;
        int end = nums.length - 1;

        while(start<=end){
            int mid = start + (end - start) / 2;
            if(target < nums[mid]){
                end = mid -1;
            } else if(target > nums[mid]){
                start = mid + 1;
            } else{
            end = mid -1;
            int mid = start + (end - start) / 2;
            if(target < nums[mid]){
                
            } else{
               return m;
            }
            int searching(int [] nums, int target, boolean findFirstIndex){
                
            }
            }
        return ans;
    }
}